rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /photos/{userId}/{imageId} {
      // Allow read if authenticated
      // Bad practice, but for the sake of time
      allow read, write: if request.auth != null;
    }
  }
}
